你的目标是搭建一个可长期运行的保研信息自动跟踪工作流。

这个工作流要定时执行，优先跟踪 Github 仓库 CS-BAOYAN/CSLabInfo2025 的更新，并筛选出与你关注方向相关的招生和实习信息。

你希望流程先做仓库同步，再做时间判断，再决定走 commit 路径还是 PR 路径，避免重复推送。

你特别强调了时间窗口逻辑。
如果最新 commit 在 1 小时内并且晚于水位线，就直接整合 commit 变动并推送，然后本轮结束，不继续跑 PR。
如果不满足这个条件，才进入 PR 增量筛选流程。

你希望用水位线机制控制增量扫描，记录上次处理时间，避免重复处理旧数据。
你希望这个水位线文件不存在时自动创建，存在时覆盖更新。

你希望脚本可维护，可迁移，不要把路径硬编码死在主逻辑里。
因此我们把配置拆到独立文件，支持通过环境变量覆盖路径。

你希望执行代码从提示词中拆出去，放到独立的 sh 文件中。
因此我们已经拆分为配置文件、提取函数文件、主流程文件。

你希望生产环境脚本存放在 ./baoyan-tracker/scripts。
提示词里也要明确部署步骤和运行命令。

你希望提示词本身尽量纯文本可读，结构清楚，不要过多 Markdown 控制符。

你希望脚本具备基础工程化能力。
包括依赖检查、异常不中断、日志统计、早退规则、静默规则、可扩展推送接口。

你希望最终输出可以用于审计。
每轮执行都需要写入 llog，记录扫描量、候选量、命中数量、过滤数量和错误数量。
无更新时要写 Idle。

你还希望我在修改过程中持续解释关键命令含义，确保你能理解每一步而不是只拿到结果。

当前你的核心诉求已经从提示词设计阶段进入可执行脚本阶段。
你现在主要在做的是可用性验证、路径与部署校准、以及推送接口的最终接入。