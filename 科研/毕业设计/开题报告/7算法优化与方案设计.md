 
图3面向水域安全的异常行为检测与智能决策思路

如图3所示，本文围绕开放水域溺水事件的发现、判定与响应，构建了一个按步骤推进的整体流程，包括目标发现与初筛、云端语义推理与溺水判定、救援决策与无人机调度、救援执行与跟踪，以及设备投放与事件归档。其中，前两阶段重点提升风险事件的发现与判别能力，后三阶段重点验证高风险事件下的辅助响应与任务执行流程。后文将围绕上述关键环节展开算法优化与方案设计。


基于无损下采样的小目标人体检测算法改进

针对开放水域场景中人体目标距离远、尺度小、纹理细节易在传统下采样过程中丢失的问题，本文拟在目标检测网络中引入基于空间转深度卷积（SPD-Conv）的无损下采样机制。该方法的核心思想是将特征图中的空间信息重排到通道维度，在降低分辨率的同时尽可能保留原始细节，其过程可概括为：

$$
X^\prime=\mathrm{SPDConv}(X)
$$

其中，$X$ 表示输入特征图，$X^\prime$ 表示经无损下采样后的输出特征图。该机制可用于替代传统步长卷积或池化层，以减轻远距离小目标特征丢失问题。空间转深度卷积的具体结构如图6所示，而图7展示了溺水检测系统的实时识别与风险预警效果。
 
图6	空间转深度卷积的结构示意图
 
图7溺水检测系统实时识别与风险预警效果展示

3.2.2 基于光流运动熵的异常行为触发机制

在目标检测结果基础上，利用稠密光流对人体区域的运动状态进行建模，并通过运动熵对异常行为进行量化分析。具体做法是先统计目标区域内不同运动幅值的分布，再计算其在时间窗口内的运动熵：

$$
H_{\mathrm{motion}}=-\sum_{i=1}^{K}p_i\log_2{\left(p_i\right)}
$$

其中，$p_i$ 表示第 $i$ 个运动幅值区间的概率分布，$H_{\mathrm{motion}}$ 表示该时间窗口内的运动熵。当运动熵超过预设阈值时，判定该时间窗口内可能存在高风险异常行为，并截取关键帧及前后时序信息上传云端。

面向人工智能物联网（AIoT）场景设计，广翼智联 FVO1 边缘智能开发板是一款基于高通 QCS6490 平台的高性能边缘计算终端，具备强大的端侧推理能力，在保证低功耗的同时能够提供高效的计算性能，适用于视频分析、智能监控等实时性要求较高的场景 。结合本课题需求，图7是本项目边缘端开发板设备主要用于承担边缘侧视觉感知与初步异常筛选任务，为后续云端语义分析与决策提供高质量输入。
 
图8	广翼智联边缘智能开发板

3.2.3 基于视觉语言模型的多模态语义消歧方法

（1）领域微调与知识注入

在通用视觉语言模型基础上，结合开放水域监控场景的领域知识进行领域自适应微调，以提升模型在特定场景下的语义判别能力。不同于传统多任务联合训练方式，本文采用以监督微调（Supervised Fine-Tuning，SFT）为主、行为判别约束为辅的优化策略，其总体损失函数可表示为：

$$
L=L_{\mathrm{SFT}}+\lambda L_{\mathrm{behavior}}
$$

其中，$L_{\mathrm{SFT}}$ 表示监督微调损失，$L_{\mathrm{behavior}}$ 表示行为判别约束损失，$\lambda$ 为平衡两者的权重系数。

（2）多模态输入构建

在多模态输入构建过程中，本文将视觉特征 `Visual_Tokens`、任务指令 `Instruction_Tokens`、领域约束 `Constraint_Tokens` 以及输出格式要求 `Format_Tokens` 进行统一组织，引导模型在理解图像内容的基础上结合任务目标和领域知识完成结构化语义判别。基于结构化约束指令的多模态语义映射原理如图9所示。
 
图9基于结构化约束指令的多模态语义映射原理示意图

在风险判定阶段，系统将边缘端检测结果与云端语义判别结果进行加权融合，得到最终风险评分；当风险评分超过预设阈值时，触发预警机制，并通过 SMTP 向管理员发送包含事件详情、风险等级、关键帧截图及位置信息的告警邮件。图10展示了系统通过 SMTP 发送预警信息至管理员邮箱的示例。
 
图10通过STMP发送预警信息至管理员邮箱示例

3.2.4 基于无人机仿真的智能调度与响应机制研究

在完成溺水风险识别与语义判别的基础上，进一步探索面向水域安全监测场景的无人机辅助响应机制。为验证视觉感知与语义判别结果在实际救援任务中的应用可行性，本文拟构建基于UE5引擎结合 AirSim + PX4 + QGC 的无人机仿真环境，对无人机巡检与应急响应过程进行模拟验证，如图11所示。需要指出的是，在实际应用中，算法应该根据无人机具体参数和硬件配置进行调整，但在本文的仿真研究阶段，主要关注方法验证和研究思路探索，通过设定合理的仿真参数来评估算法的可行性。
 
图11 AirSim+PX4+QSC无人机仿真环境

在该仿真环境中，AirSim 用于构建水域三维仿真场景并提供物理引擎与传感器数据接口，PX4 作为无人机飞控系统实现飞行控制与任务执行，QGC 用于实现任务规划与飞行状态监控。系统在检测到高风险溺水事件后，根据视觉检测得到的目标位置信息与语义分析结果，生成无人机调度指令，并通过 MAVLink 通信协议发送至飞控系统，驱动无人机执行巡检与目标跟踪任务。

（1）无人机任务调度模型

考虑到开放水域监控场景中可能存在多个无人机协同工作，或同时检测到多起溺水事件的情况，本文提出简单的任务调度策略。在本系统中，无人机部署在监控附近，当检测到高风险溺水事件后，立即触发无人机起飞执行救援任务。对于多无人机或多事件场景，可构建任务调度优先级函数，综合考虑风险等级、距离和电量：

$$
P_{\mathrm{task}}=\omega_1\cdot R+\omega_2\cdot\left(1-\widetilde{D}\right)+\omega_3\cdot\widetilde{S}
$$

其中，$R$ 表示风险等级，$\widetilde{D}$ 表示归一化距离，$\widetilde{S}$ 表示归一化电量，$\omega_1$、$\omega_2$、$\omega_3$ 为对应权重系数。在本研究的仿真验证中，仅考虑单无人机单事件的简化场景，当检测到高风险溺水事件且无人机电量充足时，无人机将起飞执行任务。

（2）视觉导航与跟踪算法

考虑到本系统中无人机部署在监控附近，溺水人员与无人机距离较近，因此不需要复杂的远距离路径规划，重点关注目标附近的视觉导航与跟踪任务。本文预计采用 YOLO 与卡尔曼滤波结合的双模跟踪机制，利用目标检测结果与运动预测信息进行融合，实现对溺水人员的持续、稳定跟踪。当 YOLO 检测置信度较高时，系统更多依赖当前观测结果；当检测置信度较低或短时失效时，系统则通过运动预测维持跟踪，从而兼顾鲁棒性、准确性与实时性。

（3）救生设备投放决策模型

本研究的仿真无人机采用底部摄像头配置，摄像头垂直向下拍摄，由于开放水域救援场景中，风速、水流等信息难以直接通过视觉传感器获取，本文提出基于视觉特征的投放决策算法，仅利用无人机视觉信息和自身姿态传感器数据进行决策。
基于单目视觉和目标检测结果进行投放时机判断。投放决策的置信度评分函数综合考虑目标居中度、尺寸匹配度和位置稳定性三个因素：

$$
C_{drop}=\omega_1\cdot C_{\mathrm{centerness}}+\omega_2\cdot C_{scale}+\omega_3\cdot C_{stability}
$$

其中，$C_{\mathrm{drop}}$ 表示投放评分，$C_{\mathrm{centerness}}$、$C_{scale}$、$C_{\mathrm{stability}}$ 分别表示目标居中度、目标尺寸匹配度和目标稳定性，$\omega_1$、$\omega_2$、$\omega_3$ 为对应权重系数。当投放评分、系统风险等级和飞行姿态同时满足阈值要求时，触发救生设备投放。

3.3 数据来源与收集方法

数据主要包括人体检测数据、水域分割数据、开放水域视频数据和仿真生成数据四部分。其中，人体检测数据主要来源于 COCO、CrowdHuman 等公开数据集，用于目标检测模型训练；水域分割数据主要来源于公开水域分割样本，以及对真实河道监控视频和公开视频抽帧后构建的河道区域样本，用于水域区域提取及人体与水域空间关系分析；开放水域视频数据主要来源于 B 站等视频平台的公开视频和可获取的真实河道监控视频，用于异常行为分析与语义判别；仿真生成数据主要来自 AirSim 等平台，用于无人机路径规划、目标跟踪和设备投放等任务验证。

数据收集时，针对公开视频和监控视频按固定时间间隔抽帧，例如每 5 秒抽取一帧；对于动作变化明显的片段，则保留关键帧序列或短视频片段。数据内容尽量覆盖河道、湖泊、水库等典型开放水域场景，并包含不同光照、距离、视角、水面反光和遮挡条件，以增强场景适应性。

3.4测试方案

为验证本文提出的溺水监测方法在开放水域场景中的有效性，实验将在模型训练环境、边缘部署环境以及无人机仿真测试环境三个层面进行验证。

在模型训练方面，深度学习模型训练与微调将在云端 GPU 计算平台上完成。计划使用 AutoDL 平台租用 NVIDIA RTX 3090 GPU 进行模型训练与推理实验，深度学习框架采用 Python 3.10 与 PyTorch 2.x。训练数据主要包括公开人体检测数据集（如 COCO、CrowdHuman）以及采集的真实河道监控视频数据，用于训练人体目标检测模型与河道实例分割模型；同时在第二阶段任务中，将结合视频片段与结构化提示词数据对 3B 参数规模的视觉语言模型（如Qwen2.5 VL等）进行领域微调，以提升模型对溺水行为语义的理解能力。

边缘部署环境方面，系统采用搭载 高通 QCS6490 芯片的广翼智联 FVO1 边缘智能开发板作为边缘计算节点，运行 Aidlux操作系统。在该平台上部署改进的小目标检测模型以及基于光流运动熵的异常行为触发模块，实现对监控视频流的实时处理与初步筛选。当检测到可能存在溺水风险的异常行为时，仅上传关键帧至云端进行语义分析。

仿真测试环境方面，本地主机预计采用 WSL2 + Ubuntu 20.04 + ROS Noetic运行环境，并基于 AirSim 无人机仿真平台构建虚拟水域巡检场景。AirSim 通过 Python API 与 PX4 飞控系统进行通信，实现无人机的起飞控制、路径规划与目标跟踪任务，同时使用QGC 进行任务规划与飞行状态监控。实验所涉及的软件版本与硬件配置将在后续实验阶段根据实际部署环境进行适当调整。
