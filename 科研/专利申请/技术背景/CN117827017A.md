---
title: "CN117827017A"
source: "https://www.patentguru.com/cn/search?q=CN117827017A"
author:
  - "[[专利顾如]]"
published:
created: 2025-12-06
description: "专利顾如平台提供关于 CN117827017A 的专利信息查询，本次查询共找到了1条关于 CN117827017A 的专利信息，专利顾如是创新者都在用的专利研发平台,专利检索,专利深度分析跟踪平台"
tags:
  - "算法与数据结构"
---
CN117827017A
摘要

本发明涉及一种基于多通道图像的防溺水检测方法、系统及存储介质，涉及计算机视觉技术领域。本发明利用语义分割模型将输入的RGB图像进行水面分割，得到水面掩膜。通过深度估计模型计算图像中每个背景像素的深度，利用深度、水面掩膜和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像。将RGB图像与距离信息图像进行拼接，得到多通道图像，并基于多通道图像构建防溺水目标检测数据集，以进行防溺水检测模型的训练。训练好的模型进行防溺水检测。多通道图像包含RGB特征和距离特征，通过在输入层融合水面距离信息和人员信息，该方法能更准确地捕捉人员与水面之间的关系，从而提高告警准确性。

权利要求

1.一种基于多通道图像的防溺水检测方法，其特征在于，包括：

利用语义分割模型，对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜；

通过深度估计模型计算水域场景的RGB图像每个像素点的深度；利用所述水面掩膜、深度和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像；

将RGB图像与水体距离信息图像拼接，得到多通道图像；

通过训练好的防溺水检测模型，对输入的多通道图像进行防溺水检测。

2.根据权利要求1所述的基于多通道图像的防溺水检测方法，其特征在于，制作水面语义分割数据集，所述水面语义分割数据集包含多种环境和条件下水域场景的RGB图像和对应RGB图像的水面掩膜标签；

以水面语义分割数据集中水面掩膜标签和语义分割模型生成的水面掩膜的IOU最大为目标，通过所述水面语义分割数据集训练语义分割模型，生成的语义分割模型对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜。

3.根据权利要求1所述的基于多通道图像的防溺水检测方法，其特征在于，制作深度估计数据集，所述深度估计数据集包含多种环境和条件下的RGB图像和深度标签；

利用所述深度估计数据集训练用于估计图像中每个像素的深度值单目深度估计模型。

4.根据权利要求1所述的基于多通道图像的防溺水检测方法，其特征在于，利用所述水面掩膜获、深度和相机内参计算每个背景像素距离最近水面的距离包括：

利用深度图像素的深度值、像素坐标和采集图像的相机内参计算各个像素点的三维坐标，其中所述相机内参包括相机的焦距和光心坐标；

利用所述水面掩膜获取水面边沿；

利用三维坐标和水面边沿计算每个背景像素距离最近水面的距离。

5.根据权利要求4所述的基于多通道图像的防溺水检测方法，其特征在于，对于深度图中的一个像素点(u,v)，其深度值为D，像素点的三维坐标(X,Y,Z)，则转换公式如下：

Z＝D；

其中，f<sub>x</sub> f<sub>y</sub>是相机的焦距，c<sub>x</sub> c<sub>y</sub>是光心坐标。

6.根据权利要求1所述的基于多通道图像的防溺水检测方法，其特征在于，对多通道图像中RGB图像进行人员入侵检测标注，即将靠近水面的人员标注为隐患人员，其余标注为正常人员，得到检测数据集标签；

修改目标检测模型的输入通道数为4，使其支持多通道图像的输入；

使用多通道图像和标签组成的防溺水数据集训练防溺水检测模型。

7.一种基于RGB图像与水体距离信息的防溺水检测系统，实现权利要求1-6任一所述的基于多通道图像的防溺水检测方法，其特征在于，包括：

图像采集模块，所述图像采集模块采集水面场景的RGB图像；

图像分割模块，所述图像分割模块利用语义分割模型，对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜；

深度估计模块，所述深度估计模块通过深度估计模型计算水域场景的RGB图像每个像素点的深度，

水体距离信息图像生成模块，所述水体距离信息图像生成模块利用所述水面掩膜、深度和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像；

多通道图像构建模块，所述多通道图像构建模块将RGB图像与水体距离信息图像拼接，得到多通道图像；

检测模块，所述检测模块通过训练好的防溺水检测模型，对输入的多通道图像进行防溺水检测。

8.一种计算机可读存储介质，所述计算机可读存储介质存储计算机程序，其特征在于，所述计算机程序被处理器执行时，实现如权利要求1-6任一项所述的基于多通道图像的防溺水检测方法。

说明书

技术领域

本发明涉及计算机视觉技术领域领域，尤其涉及一种基于多通道图像的防溺水检测方法、系统及存储介质。

背景技术

近年来，溺水已经成为全国主要的意外死亡因素之一，特别是在儿童和青少年群体中，这一问题尤为突出。很多情况下，这些不幸事件都是由于缺乏高效和可靠的防溺水报警机制，从而导致救助不及时，最终酿成悲剧。

当前，防溺水的主要手段依赖于人工巡查，这一方法存在几个明显的缺陷。首先，人工巡查效率低下，需要大量的人力物力投入，并且经常出现时间和地域的空档期，无法实现全面覆盖。其次，缺乏实时监测和告警机制，这在一定程度上阻碍了对溺水事件的及时发现和救助，从而增加了潜在风险。

此外，虽然目前有些技术尝试通过目标检测和电子围栏等手段进行危险水域的监控，如专利CN 116246417 A和CN 116527850 A，但这些方法通常由于没有结合人员与危险水域的实际距离信息，导致检测精度不高，容易出现误判和漏检。

综上所述，解决如何准确、高效地进行溺水风险的实时监测与预警，以减少意外事故，提高公共安全，是目前计算机视觉与公共安全领域内技术人员亟待解决的问题。

发明内容

为了解决上述技术问题或者至少部分地解决上述技术问题，本发明提供一种基于多通道图像的防溺水检测方法、系统及存储介质。

第一方面，本发明提供一种基于多通道图像的防溺水检测方法，包括：

利用语义分割模型，对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜；

通过深度估计模型计算水域场景的RGB图像每个像素点的深度，利用所述水面掩膜、深度和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像；

将RGB图像与水体距离信息图像拼接，得到多通道图像；

通过训练好的防溺水检测模型，对输入的多通道图像进行防溺水检测。

更进一步地，制作水面语义分割数据集，所述水面语义分割数据集包含多种环境和条件下水域场景的RGB图像和对应RGB图像的水面掩膜标签；

以水面语义分割数据集中水面掩膜标签和语义分割模型生成的水面掩膜的IOU最大为目标，通过所述水面语义分割数据集训练语义分割模型，生成的语义分割模型对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜。

更进一步地，制作深度估计数据集，所述深度估计数据集包含多种环境和条件下的RGB图像和深度标签；

利用所述深度估计数据集训练用于估计图像中每个像素的深度值单目深度估计模型。

更进一步地，利用所述水面掩膜获、深度和相机内参计算每个背景像素距离最近水面的距离包括：

利用深度图像素的深度值、像素坐标和采集图像的相机内参计算各个像素点的三维坐标，其中所述相机内参包括相机的焦距和光心坐标；

利用所述水面掩膜获取水面边沿；

利用三维坐标和水面边沿计算每个背景像素距离最近水面的距离。

更进一步地，对于深度图中的一个像素点(u,v)，其深度值为D，像素点的三维坐标(X,Y,Z)，则转换公式如下：

Z＝D；

其中，f<sub>x</sub> f<sub>y</sub>是相机的焦距，c<sub>x</sub> c<sub>y</sub>是光心坐标。

更进一步地，对多通道图像中RGB图像进行人员入侵检测标注，即将靠近水面的人员标注为隐患人员，其余标注为正常人员，得到检测数据集标签；

修改目标检测模型的输入通道数为4，使其支持多通道图像的输入；

使用多通道图像和标签组成的防溺水数据集训练防溺水检测模型。

第二方面，本发明提供一种基于RGB图像与水体距离信息的防溺水检测系统，实现所述的基于多通道图像的防溺水检测方法，包括：

图像采集模块，所述图像采集模块采集水面场景的RGB图像；

图像分割模块，所述图像分割模块利用语义分割模型，对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜；

深度估计模块，所述深度估计模块通过深度估计模型计算水域场景的RGB图像每个像素点的深度，

水体距离信息图像生成模块，所述水体距离信息图像生成模块利用所述水面掩膜、深度和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像；

多通道图像构建模块，所述多通道图像构建模块将RGB图像与水体距离信息图像拼接，得到多通道图像；

检测模块，所述检测模块通过训练好的防溺水检测模型，对输入的多通道图像进行防溺水检测。

第三方面，本发明提供一种计算机可读存储介质，所述计算机可读存储介质存储计算机程序，其特征在于，所述计算机程序被处理器执行时，实现所述的基于多通道图像的防溺水检测方法。

本发明实施例提供的上述技术方案与现有技术相比具有如下优点：

本发明利用语义分割模型将输入的RGB图像进行水面分割，得到水面掩膜。其次，通过深度估计模型计算图像中每个背景像素的深度，利用深度、水面掩膜和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像。然后，将RGB图像与距离信息图像进行拼接，得到多通道图像，并基于这种多通道图像构建防溺水目标检测数据集。最后，使用该数据集进行防溺水检测模型的训练，并用训练好的模型对新的多通道图像进行防溺水检测。本发明的多通道图像包含RGB特征和距离特征，基于多通道图像训练防溺水检测模型，通过在输入层融合水面距离信息和人员信息，该方法能更准确地捕捉人员与水面之间的关系，从而提高告警准确性。

附图说明

此处的附图被并入说明书中并构成本说明书的一部分，示出了符合本发明的实施例，并与说明书一起用于解释本发明的原理。

为了更清楚地说明本发明实施例或现有技术中的技术方案，下面将对实施例或现有技术描述中所需要使用的附图作简单地介绍，显而易见地，对于本领域普通技术人员而言，在不付出创造性劳动性的前提下，还可以根据这些附图获得其他的附图。

图1为本发明实施例提供的一种基于多通道图像的防溺水检测方法的流程图；

图2为输入RGB图像的示例图；

图3为输入RGB图像的水面掩膜的示例图；

图4为本发明实施例提供的Lite-Mono单目深度估计网络深度网络的结构图；

图5为本发明实施例提供的扩张卷积块和本地全局特征交互块的示意图。

图6为输入RGB图像的深度图的示例图；

图7为本发明实施例提供的防溺水检测模型的检测结果的示意图；

图8为本发明实施例提供的一种基于RGB图像与水体距离信息的防溺水检测系统的示意图；

具体实施方式

为使本发明实施例的目的、技术方案和优点更加清楚，下面将结合本发明实施例中的附图，对本发明实施例中的技术方案进行清楚、完整地描述，显然，所描述的实施例是本发明的一部分实施例，而不是全部的实施例。基于本发明中的实施例，本领域普通技术人员在没有做出创造性劳动的前提下所获得的所有其他实施例，都属于本发明保护的范围。

需要说明的是，在本文中，术语“包括”、“包含”或者其任何其他变体意在涵盖非排他性的包含，从而使得包括一系列要素的过程、方法、物品或者装置不仅包括那些要素，而且还包括没有明确列出的其他要素，或者是还包括为这种过程、方法、物品或者装置所固有的要素。在没有更多限制的情况下，由语句“包括一个……”限定的要素，并不排除在包括所述要素的过程、方法、物品或者装置中还存在另外的相同要素。

实施例1

参阅图1所示，本发明实施例提供一种基于多通道图像的防溺水检测方法，包括：

利用语义分割模型，对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜。

具体实施过程中，所述语义分割模型通过如下过程构建：

制作水面语义分割数据集，为增强模型鲁棒性，搜集包含多种环境和条件下水域场景的RGB图像，对RGB图像中的水面区域进行标注得到对应RGB图像的水面掩膜标签。

以水面语义分割数据集中水面掩膜标签和语义分割模型生成的水面掩膜的IOU最大为目标，通过所述水面语义分割数据集训练语义分割模型，生成的语义分割模型对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜。在一种实施方式中，使用deeplabv3+作为语义分割模型架构，对图2所表示RGB图像生成如图3所示的。

通过深度估计模型计算水域场景的RGB图像每个像素点的深度，具体实施过程中，所述深度估计模型通过如下过程构建：

制作深度估计模型数据集，为增强模型鲁棒性，搜集包含多种环境和条件下的RGB图像，对深度进行标注得到深度标签。

利用所述深度估计数据集训练用于估计图像中每个像素的深度值单目深度估计模型。

在一种实施方式中，使用Lite-Mono单目深度估计网络作为深度估计模型架构。

参阅图4和图5所示，Lite-Mono包括编码器解码器架构的深度网络，深度网络的编码器提取四个阶段的图像特征，解码器部分，使用双线性上采样来增加空间维度，并使用卷积层来连接来自编码器的三个阶段的特征。跟着预测头分别以全分辨率、1/2和1/4的分辨率输出逆深度图。

参阅图4所示，所述深度网络第一阶段包含卷积层，卷积层输出与输入图像的平均池化特征融合后输入第二阶段，第二阶段、第三阶段和第三阶段的网络结构一致，均包括依次连接的下采样层、扩张卷积块和本地全局特征交互块。第二阶段的下采样结果、第二阶段的输出和输入图像的平均池化特征融合后输入第三阶段；第三阶段的下采样结果、第三阶段的输出和输入图像的平均池化特征融合后输入第四阶段。第二阶段、第三阶段和第四阶段的输出传递给解码器部分的相应层级的上采样块，上采样块跟着预测头分别以全分辨率、1/2和1/4的分辨率输出逆深度图。

其中，参阅图5所示，扩张卷积块包含若干重复的扩张卷积模块，所述扩张卷积模块包含卷积核3×3的扩张卷积层、批归一化层、point-wise卷积层、GeLu激活层和point-wise卷积层，所述扩张卷积模块采用分支结构，将扩张卷积层、批归一化层、point-wise卷积层、GeLu激活层和point-wise卷积层提取的特征与输入特征相加。所述本地全局特征交互块包含互协方差注意力、线性归一化层、point-wise卷积层、GeLu激活层和point-wise卷积层。所述互协方差注意力对输入进行线性归一化，然后通过三个线性层分别提取KQV特征，计算K和Q之间的交叉协方差矩阵，交叉协方差矩阵经softmax归一化后来加权V，互协方差注意力采用分支结构，加权后的特征经线性层处理后与互协方差注意力的输入融合。

利用所述水面掩膜、深度和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像。

利用所述水面掩膜、深度和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像包括：

使用语义分割模型和单目深度估计模型对输入的RGB图像进行水面分割，生成水面掩膜和深度图，其中输入图像如图2所示，水面掩膜如图3所示、深度图如图6所示。

利用深度图像素的深度值、像素坐标和采集图像的相机内参计算各个像素点的三维坐标，其中所述相机内参包括相机的焦距和光心坐标；具体的，对于深度图中的一个像素点(u,v)，其深度值为D，像素点的三维坐标(X,Y,Z)，则转换公式如下：

Z＝D；

其中，f<sub>x</sub> f<sub>y</sub>是相机的焦距，c<sub>x</sub> c<sub>y</sub>是光心坐标。

利用所述水面掩膜获取水面边沿。

利用三维坐标和水面边沿计算每个背景像素距离最近水面的距离。具体的，对于非水面区域的每一个像素点P，计算距离它最近的水体区域的点Q。使用欧氏距离计算：

其中d<sub>0</sub>为最大距离阈值，这里d<sub>0</sub>设置为2米。

将这个距离值d填充到距离信息图的相应像素位置，得到水体距离信息图像。

将RGB图像与水体距离信息图像拼接，得到多通道图像；RGB图像包含3个通道的特征，水体距离信息图像包含一个通道的特征，拼接后的多通道图像包含4个通道的特征。

通过训练好的防溺水检测模型，对输入的多通道图像进行防溺水检测。对检出的靠近水域的隐患人员进行告警，如图7所示。

具体实施过程中，所述防溺水检测模型构建过程如下：

对多通道图像中RGB图像进行人员入侵检测标注，即将靠近水面的人员标注为隐患人员，其余标注为正常人员，得到检测数据集标签；

修改目标检测模型的输入通道数为4，使其支持多通道图像的输入；

使用多通道图像和标签组成的防溺水数据集训练防溺水检测模型。在一种实施方式中，使用yolov8作为防溺水检测模型架构。

实施例2

参阅图8所示，本发明实施例提供一种基于RGB图像与水体距离信息的防溺水检测系统，实现所述的基于多通道图像的防溺水检测方法，包括：

图像采集模块，所述图像采集模块采集水面场景的RGB图像；

图像分割模块，所述图像分割模块利用语义分割模型，对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜；

深度估计模块，所述深度估计模块通过深度估计模型计算水域场景的RGB图像每个像素点的深度；

水体距离信息图像生成模块，所述水体距离信息图像生成模块利用所述水面掩膜、深度和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像；

多通道图像构建模块，所述多通道图像构建模块将RGB图像与水体距离信息图像拼接，得到多通道图像；

检测模块，所述检测模块通过训练好的防溺水检测模型，对输入的多通道图像进行防溺水检测。

实施例3

本发明实施例提供一种计算机可读存储介质，所述计算机可读存储介质存储计算机程序，所述计算机程序被处理器执行时，实现所述的基于多通道图像的防溺水检测方法。

所述方法包括：

利用语义分割模型，对采集的水域场景的RGB图像进行水面分割，生成区分RGB图像水面和非水面的水面掩膜；

通过深度估计模型计算水域场景的RGB图像每个像素点的深度，利用所述水面掩膜、深度和相机内参计算每个背景像素距离最近水面的距离，并生成水体距离信息图像；

将RGB图像与水体距离信息图像拼接，得到多通道图像；

通过训练好的防溺水检测模型，对输入的多通道图像进行防溺水检测。

当然，本发明实施例所提供的一种计算机可读存储介质，其存储的计算机程序不限于如上所述的方法操作，还可以执行本发明任意实施例所提供的一种基于多通道图像的防溺水检测方法中的相关操作。

在本发明所提供的实施例中，应该理解到，所揭露的结构和方法，可以通过其它的方式实现。例如，以上所描述的结构实施例仅仅是示意性的，例如，所述单元的划分，仅仅为一种逻辑功能划分，实际实现时可以有另外的划分方式，例如多个单元或组件可以结合或者可以集成到另一个系统，或一些特征可以忽略，或不执行。另一点，所显示或讨论的相互之间的耦合或直接耦合或通信连接可以是通过一些接口，结构或单元的间接耦合或通信连接，可以是电性，机械或其它的形式。

所述作为分离部件说明的单元可以是或者也可以不是物理上分开的，作为单元显示的部件可以是或者也可以不是物理单元，即可以位于一个地方，或者也可以分布到多个网络单元上。可以根据实际的需要选择其中的部分或者全部单元来实现本实施例方案的目的。

另外，在本发明各个实施例中的各功能单元可以集成在一个处理单元中，也可以是各个单元单独物理存在，也可以两个或两个以上单元集成在一个单元中。上述集成的单元既可以采用硬件的形式实现，也可以采用软件功能单元的形式实现。

以上所述仅是本发明的具体实施方式，使本领域技术人员能够理解或实现本发明。对这些实施例的多种修改对本领域的技术人员来说将是显而易见的，本文中所定义的一般原理可以在不脱离本发明的精神或范围的情况下，在其它实施例中实现。因此，本发明将不会被限制于本文所示的这些实施例，而是要符合与本文所申请的原理和新颖特点相一致的最宽的范围。