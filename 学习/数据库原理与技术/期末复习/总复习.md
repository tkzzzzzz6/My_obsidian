# 关系数据库规范理论

## 规范化与数据依赖

### 关系模式与数据依赖

- 关系模式: R(U, D, DOM, F, I)
  - U: 属性集合
  - D: 属性域集合
  - DOM: 属性到域的映射
  - F: 函数依赖集合
  - I: 关系实例集合
- 数据依赖类型
  - 函数依赖 (FD): X -> Y
  - 多值依赖 (MVD): X ->> Y

### 函数依赖的分类

- 平凡/非平凡函数依赖
  - 平凡: Y ⊆ X (如 AB -> A)
  - 非平凡: Y ⊄ X
- 完全/部分函数依赖
  - 完全: Y 完全依赖于码 X
  - 部分: Y 依赖于 X 的真子集
- 传递函数依赖
  - X -> Y, Y -> Z, 且 Y 不是码, 则 X -> Z 为传递依赖

### 关键概念

- 超码: 能唯一确定元组的属性集
- 候选码: 最小超码
- 主属性: 出现在某个候选码中的属性
- 非主属性: 不在任何候选码中的属性
- 决定因子: 函数依赖左部

## 关系模式的规范化

### 范式层级

- 1NF ⊆ 2NF ⊆ 3NF ⊆ BCNF ⊆ 4NF ⊆ 5NF
- 目的: 消除插入异常、删除异常、修改复杂、数据冗余

### 1NF

- 关系中所有属性都是不可再分的基本数据项

### 2NF

- 属于 1NF
- 每个非主属性完全函数依赖于任何一个候选码
- 不满足 2NF 的典型问题
  - 插入异常
  - 删除异常
  - 数据冗余
  - 修改异常

### 3NF

- 属于 2NF
- 非主属性不传递函数依赖于候选码

### BCNF

- 属于 1NF
- 每个决定因子都包含候选码
- 消除主属性对非码的部分/传递依赖

### 规范化流程(记忆版)

- 属性不可分 -> 1NF
- 消除非主属性对码的部分依赖 -> 2NF
- 消除非主属性对码的传递依赖 -> 3NF
- 消除主属性对非码的部分/传递依赖 -> BCNF

### 分解的两个性质

- 无损连接: 分解后可通过连接恢复原关系
- 依赖保持: 分解后依赖可由各子关系的依赖推出

# 关系数据库标准语言 SQL

## SQL 特点

- 综合统一
- 高度非过程化
- 面向集合的操作方式
- 一套语法支持交互与嵌入式
- 简单易学
- 支持 DDL / DML / DCL

## 数据定义 (DDL)

### 创建基本表

```sql
create table sc (
    sno char(9),
    cno char(4),
    grade smallint,
    primary key (sno, cno),
    foreign key (sno) references student(sno),
    foreign key (cno) references course(cno)
);
```

### 修改/删除表 (常见写法)

```sql
alter table student add sphone char(11);
alter table student drop column sphone;
drop table sc;
```

### 索引

```sql
create unique index stusno on student(sno);
create unique index coucno on course(cno);
create unique index scno on sc(sno asc, cno desc);
drop index scno;
```

## 数据查询 (DQL)

### 单表查询

```sql
select sname, sage from student;
select * from student;
select 2025 - sage as byear from student;
select lower(sdept) from student;
select sname, sage from student where sage < 20;
select sname, sage from student where sage between 20 and 30;
select sname, sage from student where sdept in ('cs', 'ee');
select sno from student where sname like '张%';
select sno from student where sname like '_棵%';
select sno from sc where grade is null;
select sname from student where sdept = 'cs' and sage < 20;
select sname from student order by sdept, sage desc;
```

### 聚集与分组

```sql
select count(*) from student;
select avg(grade) from sc;
select max(grade) from sc where cno = '1';
select cno, count(sno) from sc group by cno;
select sno from sc group by sno having count(cno) > 3;
```

- where 子句中不能出现聚集函数
- 分组后的条件使用 having

### 连接查询

```sql
select student.*, sc.*
from student, sc
where student.sno = sc.sno;

select s.*, sc.cno, sc.grade
from student as s
left join sc on s.sno = sc.sno;

select student.sno, student.sname, course.cname, sc.grade
from student
left join sc on student.sno = sc.sno
left join course on sc.cno = course.cno;
```

## 数据更新 (DML)

```sql
insert into sc(sno, cno, grade) values ('20230001', '0001', 95);
update sc set grade = 96 where sno = '20230001' and cno = '0001';
delete from sc where grade is null;
```

## 视图

```sql
create view cs_student as
select sno, sname, sdept
from student
where sdept = 'cs';

drop view cs_student;
```

## 完整性

- 实体完整性: 主码非空且唯一
- 参照完整性: 外码与主码一致或为空
- 用户定义完整性: check/触发器等

# 复习速记

- 2NF 解决: 非主属性对码的部分依赖
- 3NF 解决: 非主属性对码的传递依赖
- BCNF 解决: 主属性对非码的依赖
- 无损连接与依赖保持是分解的核心标准
