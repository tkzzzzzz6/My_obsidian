1. 欧几里得算法求最大公约数
```cpp
eucild(m,n)
while n != 0 do
    t = m mod n
    m = n
    n = t
return m
```
2. 连续整数检测算法求最大公约数
```cpp
function(m,n)
    t=min(m,n)
    while t>=0 do 
        if m mod t == 0 and n mod t == 0
            return t
        --t;
```
3. 素数筛算法
```cpp
function(n)
    for p = 2 to n do
        A[p] = p
    for p = 2 to sqrt(n) do
        if A[p] != 0
            j = p*p
        while j < n do
            A[j] = 0
            j = j + p
    i = 0
    for p = 2 to n do
        if A[p] != 0 
            L[i] = A[p]
            ++i
    return L
```
4. 矩阵乘积
```cpp
function(A[0..n-1][0..n-1],B[0..n-1][0..n-1])
    for i = 0 to n-1 do
        for j = 0 to n-1 do 
            C[i][j] = 0
            for k = 0 to n-1 do
                C[i][j] += A[i][k]*B[k][j]
    return C
```
5. 汉诺塔
function(n,A,B,C)
    if n > 0

6. 选择排序
7. 冒泡排序
8. 字符串蛮力匹配
9.  蛮力平面距离最近两点
10. 深度优先搜索遍历
11. 广度优先搜索遍历
12. 插入排序
13. 拓扑排序
14. 生成排列算法
15. 反射格雷码
16. 折半查找
17. 俄式乘法
18. 约瑟夫环问题
19. 三重查找
20. Lomuto划分
21. 快速选择
22. 快速排序
23. 归并排序
24. 归并排序Merge
25. 二叉树遍历
26. 高斯消去法
27. 构造堆
28. 霍纳法则
29. 从左至右二进制幂
30. 从右至左二进制幂
31. 比较计数排序
32. 分布计数排序
33. 填充移动表
34. Horspool字符串匹配算法
35. 币值最大话问题
36. 找零问题
37. 硬币收集问题
38. 背包记忆化
39. 最优二叉查找树
40. Warshall算法
41. Floyd算法
42. 最小生成树Prim算法
43. 最小生成树Kruskal算法
44. 最短路径Dijkstra算法
45. 平分法求方程$x^3+x-1=0$的根
46. 试位法求方程$x^3+x-1 = 0$的根
47. 牛顿法求方程$x^3+x-1 = 0$的根
48. 贪心法求田忌赛马问题
49. 用霍纳法则求一个多项式在一个给定点的值
50. 用来生成排列的Johnson-Trotter算法
51. 韩信点兵
52. 循环赛