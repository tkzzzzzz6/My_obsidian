逻辑设备与物理设备

逻辑设备表 LUT

缓冲管理

差错控制

独立设备的分配与回收

## 用户层的 IO 软件
用户态,内核态
系统调用与库函数

## 缓冲区管理

无缓存

单缓存:$max(c,t) + m$

双缓存:$max(c+m,t)$

单工,双工传输

环形缓冲区

磁盘存储器管理与调度

早期磁盘调度算法

- fcfs
- sstf

基于扫描的磁盘调度算法

  scan 算法(电梯调度算法):上下移动,两个方向
  cscan 算法:只有一个方向
  Nstep-scan 算法:每次移动固定的距离,避免 armstickiness